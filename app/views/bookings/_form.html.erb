<%= simple_form_for(@booking, :html => { :class => 'form-vertical'}) do |f| %>
	<%= f.input :description %>
	<%= f.input :start_date %>
	<%= f.input :start_time %>
	<%= f.input :end_date %>
	<%= f.input :end_time %>
	<%= f.input :venue_id, :as => :hidden %>
  	<%= f.button :submit %>
<% end %>
